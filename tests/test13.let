fnc init
    /* rule 110 */
    mem 28 + 1 .<

    27 0 while
        2dup < if
            stop
        else
	    27 0 while
	        2dup < if
		    stop
		else
		    dup mem + .> if
		        mem 30 + 42 .<
		    else
		        mem 30 + 32 .<
		    elend
		    1 mem 30 + 1 1 syscall

                    1 +
		elend
	    whend
	    pop pop

            mem 30 + 10 .<
	    1 mem 30 + 1 1 syscall

            mem .> 1 shl
	    mem 1 + .>
	    or

            1 while
	        28 over < if
		    stop
		else
		    swap 1 shl 7 and
		    over mem + 1 + .> or
		    2dup 110 swap shr 1 and
		    swap mem + swap .<
		    swap

		    1 +
		elend
            whend
	    pop pop

            1 +
	elend
    whend
    pop pop

    0 exit
end